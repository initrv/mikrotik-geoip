services:
  mikrotik-geoip:
    build: .
    container_name: mikrotik-geoip
    restart: unless-stopped
    ports:
      - "8083:8000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000"]
      interval: 30s
      timeout: 10s
      retries: 3
